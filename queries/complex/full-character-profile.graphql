query FullCharacterProfile($ID: ID!) {
  person(id: $ID) {
    name
    # Show character name
    name
    filmConnection {
      films {
        title
      }
    }
    starshipConnection {
      starships {
        name
      }
    }
    homeworld {
      name
    }
  }
}
